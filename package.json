{
  "name": "metismenu",
  "version": "2.7.4",
  "description": "A jQuery menu plugin",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "assemble"
  },
  "license": "MIT",
  "devDependencies": {
    "assemble": "^0.24.3",
    "gulp-extname": "^0.2.2",
    "helper-markdown": "^1.0.0"
  }
}